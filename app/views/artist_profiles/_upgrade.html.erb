
      <div class="modal-header bg-secondary bg-opacity-25">
        <h5 class="modal-title" id="exampleLabel">Payment Information</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">

       <!--  <%#= f.hidden_field :payment_method, id: 'payment_method' %>

          <div class="actions" id="submission">
            <%#= f.submit %>
          </div>
        <%# end %> -->
      </div>



<!-- <script src="https://js.braintreegateway.com/web/dropin/1.32.1/js/dropin.min.js"></script>

<script>
  var form = $('#transaction-form');
  var submitBtn = $('#submission');
  var client_token = "<%= @client_token %>";

  braintree.dropin.create({
    authorization: client_token,
    selector: '#dropin-container'
  }, function (err, dropinInstance) {
    if (err) {
      console.log('braintree Error',err);
    }

    dropinInstance.requestPaymentMethod(function (err, payload) {
      if (err) {
        console.log('payload Error', err);
      }

      $('#payment_method').val(payload.nonce);
      console.log($('#payment_method').val());
    });

    // submitBtn.on('click', function (event) {
    //   event.preventDefault();
    //   console.log("in");

    // });
  });

</script>
 -->
